/**
 * This is just a helper for initializing the testProperty. 
 * Since Istanbul currently only supports .js files (and not .html), only this code will show up in the code coverage report.
 *
 * @class getInitialValue
*/
function getInitialValue() {
	console.log ("Hello from test.js");
	return "this is the initial value";
}